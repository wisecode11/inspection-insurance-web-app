/** Legacy mock retained for offline demos — templates now use live `/templates` and `/checklists` APIs. */

export function getTemplatesMock() {
  return null
}

export function saveTemplatesMock() {
  return null
}
